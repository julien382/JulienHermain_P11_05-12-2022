import SectionImageTitle from '../SectionImageTitle/SectionImageTitle'
import './SectionImage.css'

const SectionImage = () => {
    return (
        <div className='sectionImage'>
            <SectionImageTitle title="Chez vous, partout et ailleurs"/>
                
        </div>
    )
}

export default SectionImage