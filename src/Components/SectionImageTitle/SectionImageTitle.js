import './SectionImageTitle.css'

const SectionImageTitle = ({title}) => {
    return (
        <p className='sectionImageTitle'>
            {title}
        </p>
    )
}

export default SectionImageTitle